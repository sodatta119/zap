# Cargo build output (workspace now lives under networking/, so match at any depth)
target/
/dist

# macOS / IDE junk
.DS_Store
.idea/

# local-only handoff notes
docs/HANDOFF.md


# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
